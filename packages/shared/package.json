{
  "name": "@pay/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {
    "@pay/config": "workspace:*",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "format": "prettier --write .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests'"
  }
}
